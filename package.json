{
  "name": "meshbreakers",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Phone-playable party roguelike. Static web game, no server.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build && node scripts/inline.mjs",
    "test": "vitest run",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^5.4.11",
    "vitest": "^2.1.8"
  }
}
